export function StorefrontFooter(): React.JSX.Element {
  return (
    <footer className="border-t border-amber-200/70">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-4 py-6 text-sm text-stone-600 sm:flex-row sm:items-center sm:justify-between sm:px-6 lg:px-8">
        <p>Pastane — taze üretim, sıcak teslimat.</p>
        <p>Europe/Istanbul</p>
      </div>
    </footer>
  );
}

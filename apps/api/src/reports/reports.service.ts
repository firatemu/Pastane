import { Inject, Injectable } from '@nestjs/common'; import { OrderStatus, PaymentStatus, ProductStatus, ReviewStatus } from '@prisma/client'; import { PrismaService } from '../database/prisma.service'; import type { QueryReportRangeDto } from './dto/query-report-range.dto';
@Injectable() export class ReportsService { constructor(@Inject(PrismaService) private readonly prisma:PrismaService){} async dashboard(){const today=new Date(); today.setHours(0,0,0,0); const [awaitingAction,inPreparation,readyForAssignment,lowStock,outOfStock,pendingReviews]=await Promise.all([this.prisma.order.count({where:{status:OrderStatus.CONFIRMED,deletedAt:null}}),this.prisma.order.count({where:{status:OrderStatus.PREPARING,deletedAt:null}}),this.prisma.order.count({where:{status:OrderStatus.READY,deliveryType:'HOME_DELIVERY',delivery:null,deletedAt:null}}),this.prisma.stockEntry.count({where:{date:today,quantity:{lte:5},deletedAt:null}}),this.prisma.product.count({where:{status:ProductStatus.OUT_OF_STOCK,deletedAt:null}}),this.prisma.review.count({where:{status:ReviewStatus.PENDING,deletedAt:null}})]); return {awaitingAction,inPreparation,readyForAssignment,lowStock,outOfStock,pendingReviews};} async sales(q:QueryReportRangeDto){const range=this.range(q); const rows=await this.prisma.order.aggregate({_sum:{grandTotal:true},_count:{_all:true},where:{status:{in:[OrderStatus.CONFIRMED,OrderStatus.PREPARING,OrderStatus.READY,OrderStatus.ASSIGNED_TO_COURIER,OrderStatus.OUT_FOR_DELIVERY,OrderStatus.DELIVERED]},createdAt:range,deletedAt:null,payments:{some:{status:PaymentStatus.SUCCESS}}}}); return {orderCount:rows._count._all,grossSales:rows._sum.grandTotal??0};} async products(q:QueryReportRangeDto){const range=this.range(q); const grouped=await this.prisma.orderItem.groupBy({by:['productId','productNameSnapshot'],_sum:{quantity:true},where:{order:{createdAt:range,status:{not:OrderStatus.CANCELLED},deletedAt:null}},orderBy:{_sum:{quantity:'desc'}},take:5}); return {topProducts:grouped};} private range(q:QueryReportRangeDto){return q.startDate||q.endDate?{...(q.startDate?{gte:new Date(q.startDate)}:{}),...(q.endDate?{lte:new Date(q.endDate)}:{})}:undefined;}}

'use client';
import { flexRender, getCoreRowModel, useReactTable, type ColumnDef } from '@tanstack/react-table';
export function DataTable<T>({data,columns,empty='Kayıt bulunamadı.'}:Readonly<{data:T[];columns:ColumnDef<T>[];empty?:string}>):React.JSX.Element{const table=useReactTable({data,columns,getCoreRowModel:getCoreRowModel()});return <div className="overflow-x-auto rounded-3xl border bg-white"><table className="min-w-full text-sm"><thead className="bg-stone-50">{table.getHeaderGroups().map(g=><tr key={g.id}>{g.headers.map(h=><th className="px-4 py-3 text-left font-semibold" key={h.id}>{h.isPlaceholder?null:flexRender(h.column.columnDef.header,h.getContext())}</th>)}</tr>)}</thead><tbody>{table.getRowModel().rows.length?table.getRowModel().rows.map(r=><tr className="border-t" key={r.id}>{r.getVisibleCells().map(c=><td className="px-4 py-3 align-top" key={c.id}>{flexRender(c.column.columnDef.cell,c.getContext())}</td>)}</tr>):<tr><td className="px-4 py-8 text-center text-stone-500" colSpan={columns.length}>{empty}</td></tr>}</tbody></table></div>}

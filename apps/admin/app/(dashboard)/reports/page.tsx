import { requirePermission } from '../../../lib/auth/guards'; import { requireAdminSession } from '../../../lib/auth/session'; import { ReportsSummary } from '../../../components/operations/reports-summary'; export default async function ReportsPage(){const s=await requireAdminSession(); requirePermission(s,['reports.sales','reports.products']); return <ReportsSummary permissions={s.permissions}/>}

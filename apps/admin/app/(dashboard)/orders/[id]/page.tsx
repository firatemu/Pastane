import { requirePermission } from '../../../../lib/auth/guards'; import { requireAdminSession } from '../../../../lib/auth/session'; import { OrderDetailPanel } from '../../../../components/operations/order-detail-panel'; export default async function OrderDetailPage({params}:{params:Promise<{id:string}>}){const s=await requireAdminSession(); requirePermission(s,['orders.viewAll']); const {id}=await params; return <OrderDetailPanel id={id} permissions={s.permissions}/>}

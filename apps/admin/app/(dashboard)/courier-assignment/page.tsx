import { requirePermission } from '../../../lib/auth/guards'; import { requireAdminSession } from '../../../lib/auth/session'; import { CourierAssignmentManager } from '../../../components/operations/courier-assignment-manager'; export default async function CourierAssignmentPage(){const s=await requireAdminSession(); requirePermission(s,['orders.assignCourier','couriers.view']); return <CourierAssignmentManager permissions={s.permissions}/>}

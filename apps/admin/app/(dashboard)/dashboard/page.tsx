import { requireAdminSession } from '../../../lib/auth/session'; import { can } from '../../../lib/permissions/can'; import { DashboardLiveMetrics } from '../../../components/dashboard/dashboard-live-metrics';
export default async function DashboardPage(){const s=await requireAdminSession(); return <section className="space-y-6"><div><p className="text-sm font-medium text-amber-700">Gösterge paneli</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Operasyon genel görünümü</h1></div>{can(s.permissions,['reports.sales'])?<DashboardLiveMetrics/>:<p className="text-sm text-stone-600">Bu rol için canlı gösterge paneli metrikleri görünür değil.</p>}</section>}

# Staging overrides for official Supabase stack (Faz 8.0 dry-run).
name: supabase-staging

networks:
  default:
    name: pastane_staging_supabase
    external: false

services:
  studio:
    container_name: pastane_supabase_studio_staging
    ports: !override
      - "127.0.0.1:55323:3000"

  kong:
    ports: !override []

  db:
    container_name: pastane_supabase_db_staging
    networks:
      default:
        aliases:
          - supabase-db
    ports: !override
      - "127.0.0.1:55432:5432"
    volumes: !override
      - ./volumes/db/realtime.sql:/docker-entrypoint-initdb.d/migrations/99-realtime.sql:Z
      - ./volumes/db/webhooks.sql:/docker-entrypoint-initdb.d/init-scripts/98-webhooks.sql:Z
      - ./volumes/db/roles.sql:/docker-entrypoint-initdb.d/init-scripts/99-roles.sql:Z
      - ./volumes/db/jwt.sql:/docker-entrypoint-initdb.d/init-scripts/99-jwt.sql:Z
      - supabase_staging_db_data:/var/lib/postgresql/data
      - ./volumes/db/_supabase.sql:/docker-entrypoint-initdb.d/migrations/97-_supabase.sql:Z
      - ./volumes/db/logs.sql:/docker-entrypoint-initdb.d/migrations/99-logs.sql:Z
      - ./volumes/db/pooler.sql:/docker-entrypoint-initdb.d/migrations/99-pooler.sql:Z
      - db-config:/etc/postgresql-custom

  supavisor:
    ports: !override []

volumes:
  supabase_staging_db_data:
    name: supabase_staging_db_data
  db-config:
    name: supabase_staging_db_config
